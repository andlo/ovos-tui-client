# ovos-tui-client

A split-pane terminal UI for testing OVOS without a mic/speaker - built
as a lightweight replacement for the unmaintained `ovos-cli-client` and
`neon-cli-client` (whose `pyyaml~=5.4` dependency fails to build on
modern Python/setuptools).

Work in progress.
